stages:
  - name: Install
      steps:
      - runScriptConfig:
            image: node:lts-alpine3.9
            shellScript: |-
              npm install

  - name: Build
    steps:
      - runScriptConfig:
          image: pivotalpa/angular-cli:latest
          shellScript: |-
            ng build --prod

