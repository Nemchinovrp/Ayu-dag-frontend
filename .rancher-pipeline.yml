stages:
  - name: Install
      steps:
          - runScriptConfig:
            image: node:8.17.0-alpine3.10
            shellScript: |-
              npm install

  - name: Build
    steps:
      - runScriptConfig:
          image: pivotalpa/angular-cli:latest
          shellScript: |-
            ng build --prod

